# bookstorePOC

This project contains both **frontend (Angular)** and **backend (Spring Boot)** parts of a Bookstore application.

---

###  Git & GitHub Steps Followed
- Opened the terminal in **VS Code**  
- Initialized Git using:
  ```bash
  git init
